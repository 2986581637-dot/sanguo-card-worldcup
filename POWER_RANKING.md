# 150人综合战力排名规则

当前排名由 `characters.js` 在页面加载时实时生成，避免静态排名表与游戏数据不一致。

- 基础属性只保留 `martial`（武力）和 `intelligence`（智力）。
- 综合战力：`power = Math.max(martial, intelligence)`。
- 按综合战力从高到低排序；同战力时依次比较智力、武力和原始名单顺序，确保 `rank` 1—150 唯一。
- Rank 1—30：天阶（每10名分为天一、天二、天三）。
- Rank 31—60：地阶（每10名分为地一、地二、地三）。
- Rank 61—90：人阶（每10名分为人一、人二、人三）。
- Rank 91—150：凡阶（每20名分为凡一、凡二、凡三）。

运行 `node balance_test.js --full` 可以输出当前完整150人排名表。
